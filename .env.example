# PostgreSQL connection used by Prisma. Matches the local docker-compose service.
# Host port 5433 is used so we don't clash with a typical system Postgres on 5432.
DATABASE_URL="postgresql://runtimecatch:runtimecatch@localhost:5433/runtimecatch?schema=public"
