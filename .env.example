# PostgreSQL connection used by Prisma. Matches the local docker-compose service.
# Host port 5435 is used so we don't clash with system Postgres installs on 5432-5434.
DATABASE_URL="postgresql://runtimecatch:runtimecatch@localhost:5435/runtimecatch?schema=public"
